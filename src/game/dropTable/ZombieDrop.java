package game.dropTable;

/* All drops the zombies can drop when killed */
public class ZombieDrop extends MonsterDrop {

    public ZombieDrop(int difficulty, double silver) {
        super(difficulty, silver);
    }

}
